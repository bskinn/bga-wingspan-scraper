{
  "scripts": {
    "format": "npx prettier -w .",
    "build": "npm run build-ts && npm run comment-built",
    "clean": "tsc --build --clean",
    "build-ts": "tsc --build",
    "comment-built": "node comment_build_files.js"
  },
  "devDependencies": {
    "prettier": "3.0.3",
    "typescript": "^5.2.2"
  }
}
