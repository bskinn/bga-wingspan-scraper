{
  "name": "bga-wingspan-scraper",
  "version": "0.1.0",
  "description": "Firefox extension to scrape BGA Wingspan game replays",
  "private": true,
  "scripts": {
    "format": "npx prettier -w .",
    "build": "run-s dev:clean dev:build dev:assets dev:manifest",
    "clean": "npm run dev:clean",
    "clean-all": "run-p dev:clean dist:clean",
    "dist": "run-s dist:clean dist:build dist:copy dist:assets dist:manifest",
    "tsc": "npx tsc --noEmit",
    "dev:build": "npx webpack --config webpack.config.dev.js",
    "dev:manifest": "node render_manifest.js build-dev",
    "dev:assets": "npx copyfiles assets/**/* build-dev",
    "dev:clean": "node -e \"fs = require('fs'); fs.rmSync('build-dev', {recursive: true, force: true})\"",
    "dist:clean": "run-s dist:clean:dist dist:clean:build",
    "dist:clean:dist": "node -e \"fs = require('fs'); fs.rmSync('dist', {recursive: true, force: true})\"",
    "dist:clean:build": "node -e \"fs = require('fs'); fs.rmSync('build', {recursive: true, force: true})\"",
    "dist:build": "npx webpack --config webpack.config.prod.js",
    "dist:copy": "npx copyfiles -f build/*.js dist",
    "dist:assets": "npx copyfiles assets/**/* dist",
    "dist:manifest": "node render_manifest.js dist"
  },
  "devDependencies": {
    "commander": "^11.1.0",
    "copyfiles": "^2.4.1",
    "npm-run-all": "^4.1.5",
    "prettier": "3.0.3",
    "ts-loader": "^9.5.0",
    "typescript": "^5.2.2",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
